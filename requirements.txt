# Backend ASR (faster-whisper) — dependencias directas pineadas.
# CTranslate2, av, numpy y tokenizers vienen como transitivas de faster-whisper.
faster-whisper==1.2.1
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# Runtime GPU: CTranslate2 necesita cuBLAS 12 + cuDNN 9. Se sirven desde estos
# wheels (no del CUDA del sistema) y se añaden a LD_LIBRARY_PATH en run-asr.sh.
# Versiones validadas en RTX PRO 6000 Blackwell (sm_120).
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.23.0.39
